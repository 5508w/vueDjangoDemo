项目搭建：

项目介绍：
    基于page object models设计模型
    pom将架构分为三层，
        页面层：封装定位方法、等待方法
        操作层：调用页面的元素，操作元素
        业务层：规划业务元素定位，服务于操作层


npm安装与配置 略

vue端
    配置vue环境
        npm install
        npm run build
    启动vue项目
        npm run dev
    详细说明文档到appfront中查看前端readme

django端
    安装与配置django环境
        确定本地的python版本，找对应的django版本(https://www.djangoproject.com/download/)，如python3.8 对应django版本3.1
        pip install Django==3.1
    启动django项目
        
    详细说明文档到book_demo中查看readme

mysql端
    对前端的配置

    对后端的配置

    本地环境搭建与启动略